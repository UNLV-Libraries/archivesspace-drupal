uuid: 376945f0-cb7d-4345-966b-acb258c86bbc
id: as_repositories
label: ArchivesSpace Repositories
migration_group: archivesspace

source:
  plugin: url
  data_fetcher_plugin: http
  data_parser_plugin: json
  authentication:
    plugin: archivesspace
    # The settings below will override those set at /admin/structure/archivesspace/settings
#    username: 'admin'
#    password: 'admin' # I *really* hope you changed this...
#    base_uri: 'http://localhost:8089'
  urls: http://localhost:8089/repositories
  ids:
    uri:
      type: string
  item_selector: 0
  fields:
    -
      name: name
      label: 'Name'
      selector: /name
    -
      name: uri
      label: 'URI'
      selector: /uri
    -
      name: repo_code
      label: 'Repository Code'
      selector: /repo_code

destination:
  plugin: entity:node
  bundle: repository

process:
  type:
    plugin: default_value
    default_value: repository
  title: name
  field_repository_short_name: repo_code
  uid:
    plugin: default_value
    default_value: 1
